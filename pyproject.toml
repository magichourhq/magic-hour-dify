[project]
name = "magic-hour-dify"
version = "0.0.1"
description = "Magic Hour image and video generation tools for Dify."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dify-plugin>=0.10.2,<0.11.0",
    "httpx>=0.28.1,<0.29.0",
]
